import pandas as pd

df = pd.read_csv("pokemon.csv")

# drop columns
df = df.drop(columns=["Legendary", "Attack"])



print(df)

# remove missing data

#df = df.dropna(subset=["Type 2"])



#print(df)

# replace empty data

df = df.fillna({"Type 2": "None"})

print(df.to_string())

# replace filled data with something else

df["Type 1"] = df["Type 1"].replace({"Grass": "GRASS", "Fire": "FiRe"})

print(df)

# standardoze text

df["Name"] = df["Name"].str.lower()

print(df)

# fix or change data types
def error():
    pass
    df["Legendary"] = df["Legendary"].astype(int)

    print(df)

# remove duplicate values

df = df.drop_duplicates()

print(df)